{
  "name": "snip-backend",
  "version": "1.0.0",
  "description": "Snip — in-memory URL shortener backend (Bun)",
  "type": "module",
  "scripts": {
    "start": "bun run server.js",
    "dev": "bun --watch server.js"
  }
}
